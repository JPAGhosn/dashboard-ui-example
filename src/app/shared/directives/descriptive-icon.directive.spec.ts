import { DescriptiveIconDirective } from './descriptive-icon.directive';

describe('DescriptiveIconDirective', () => {
  it('should create an instance', () => {
    const directive = new DescriptiveIconDirective();
    expect(directive).toBeTruthy();
  });
});
