import { ActionIconDirective } from './action-icon.directive';

describe('RightIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionIconDirective();
    expect(directive).toBeTruthy();
  });
});
